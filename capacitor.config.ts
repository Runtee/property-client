import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.venta.land',
  appName: 'venta',
  webDir: 'dist/client-app/browser'
};

export default config;
